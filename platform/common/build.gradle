apply from: "../../GradleCommon/common.gradle"

repositories {
    // Additional repositories
}

dependencies {
    shadow (project(":AuraBan-api")) {
        exclude module: "lombok"
    }
    
    shadowRelocate "me.lucko:jar-relocator:${jarRelocator_version}"
}
